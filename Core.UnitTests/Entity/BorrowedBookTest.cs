namespace Core.UnitTests.Entity;

public class BorrowedBookTest
{
    // [Fact]
    // public void ExtendDueDate_WhenUserHasBorrowedBook_ShouldExtendDueDate()
    // {
    //     var user = User.Create("user@mail.com", "password", Role.Create(RoleType.Admin));
    //     var book = Book.CreateBook("111111111111", "aa", new CategoryId(Guid.NewGuid()), "shkar", 4);
    //     var borrowedDate = DateTime.Now.AddDays(4);
    //     book.Borrow(user,borrowedDate);
    //     Assert.Single(user.BorrowedBooks);
    //     var borrowedBook = user.BorrowedBooks.First();
    //     var newBorrowedDate=DateTime.Now.AddDays(5);
    //     borrowedBook.ExtendDate(newBorrowedDate);
    //     Assert.Equal(borrowedBook.DueDate,newBorrowedDate);
    //     
    // }
}